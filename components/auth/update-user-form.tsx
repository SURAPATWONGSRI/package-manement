"use client";

interface UserUpdateFormProps {
  name: string;
  image: string | null;
}

export const UserUpdateForm = ({ name, image }: UserUpdateFormProps) => {
  return <div>UserUpdateForm</div>;
};
